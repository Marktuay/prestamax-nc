// Aplicacion realizada en Javascript para realizar calculo financieron de prestamo.

let  principal = '';
let interes = '';
let cuotas = '';

// función de calculo de prestamos segun las variables y la formula del prestamo

function calculo(params) {
    
}

